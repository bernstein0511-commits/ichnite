# docker-compose.yaml のMySQL設定と対応済みのため、通常はそのまま変更不要
DB_HOST=mysql
DB_PORT=3306
DB_NAME=ichnite
DB_USER=root
DB_PASSWORD=root

# 任意：未設定でもマーカーの作成・保存・記録帳などコア機能は問題なく動作する。
# 設定した場合のみ、マーカーのAI解説（explanation等）が生成されるようになる。
OPENAI_API_KEY=
